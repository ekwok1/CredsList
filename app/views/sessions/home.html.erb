<div class="container-fluid">
  <div>Listings</div>
  <% @items.each do |item| %>
    <div><%= item.name %></div>
    <div><%= item.description %></div>
    <div><%= item.price %></div>
    <img src="<%= item.pic_url %>">
  <% end %>
</div>
