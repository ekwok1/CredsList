<h1>Change Your Password</h1>
<%= form_for :user, url:password_reset_path(@user.password_reset_token), method: :patch do |f|%>
<%= f.label :password %>
<%= f.password_field :password, autofocus: true%>

<%= f.submit "Reset Password"%>

<% end %>